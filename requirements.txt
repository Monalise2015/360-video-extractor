# Video 360 Frame Extractor - Python dependencies
# Las instala automáticamente video360_frame_extractor.py al primer arranque,
# pero puedes hacerlo manual con: pip install -r requirements.txt

piexif>=1.1.3        # Inyección GPS en EXIF (thread-safe)
Pillow>=9.0.0        # Manipulación de imágenes
numpy>=1.21.0        # Arrays para fusión IMU
imufusion>=1.2.0     # Madgwick AHRS para heading desde giroscopio INSV

# Sistema (no se instalan por pip):
#   - FFmpeg / ffprobe   (extracción de frames y metadata video)
#   - Python 3.8+        (recomendado 3.10+)
#   - tkinter            (GUI; instalado con Python en Windows/Mac, en Linux: python3-tk)
